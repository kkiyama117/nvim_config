-- lua_add {{{
-- }}}
-- lua_source {{{
-- Set term_gui_colors again
vim.opt.termguicolors = true
-- replace system `vim.notify` by this plugin
local plugin = require('notify')
plugin.setup()
vim.notify = plugin

-- TODO: Add multiple hooks with `ddu` and get `require("notify").history()` as a source of ddu
-- }}}

