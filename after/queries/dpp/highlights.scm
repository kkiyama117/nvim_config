; dpp host-level highlights. Injected lua blocks are tokenized by the lua
; language's own highlights (kakehashi resolves injected regions against the
; injection language's queries), so no `; inherits: lua` is needed here.

(start_marker_line) @comment
(end_marker_line) @comment
(wrapper_start_line) @comment
(wrapper_end_line) @comment
