# Review: deps-docs-auto-gen pass 2 — Reviewer-C Plugin loading

Reviewed: docs/specifications/implementation/2026-07-13-deps-docs-auto-gen-design.md
Reviewer: composer-2.5-fast (Reviewer-C)
Date: 2026-07-13
Pass: 2
Status: in-review

## Verdict

Approve as-is. All five pass-1 blocking findings are RESOLVED with no regression. The A2/C2/D1-cross resolution (Phase 0 hand-edit) is correct: the post-Phase-0 `normal_deps` list exactly matches §5.5's generated sample and `deps/dpp.toml` file order (`dpp-ext-toml`, `dpp-ext-local`, `dpp-ext-installer`, `dpp-ext-packspec`, `dpp-protocol-git`, `dpp-protocol-http`, `vim-denops/denops.vim` last). Eagerly loading `dpp-protocol-http` at bootstrap is safe — `load_plugins` (`dpp_loader.lua:70-78`) only clones + `runtimepath:prepend` (line 76); it does NOT `:runtime!` `plugin/*.vim` files, so no sourced side effects fire early, and the plugin is symmetric with `dpp-protocol-git` (already eagerly loaded today). C4 (denops.vim last) is preserved in both Phase 0 and §5.5. `dpp.make_state` input (lines 107/119) is unchanged — it takes `(dpp_cache_home, dpp_denops_script)`, not the Lua lists.

No new CRITICAL/HIGH findings. Two INFO-level wording nits (C-pass2-1, C-pass2-2) are noted but do not block.

## Pass-1 blocking findings — resolution status

| ID | Pass-1 severity | Pass-2 status | Note |
|----|-----------------|---------------|------|
| A1 | CRITICAL | RESOLVED | `lua/dpp_min_deps.lua` + `require("dpp_min_deps")` (dot-less) throughout design §1.2 S2, §3.1, §4 Phase 1, §5.5, I4. Grep for `dpp_min_deps.generated` / `dpp_min_deps/generated` returns matches only in pass-1 review files + pass-2 prompt (historical); zero in design/spec/issue. `require("dpp_min_deps")` resolves to `lua/dpp_min_deps.lua` per `:help lua` (no dot → direct filename match). Generated signal carried by `-- AUTO-GENERATED by ...` header comment per R8, not filename. (Reviewer-A's angle; confirmed from C side: no rtp/plugin-loading implications.) |
| A2/C2/D1-cross | HIGH | RESOLVED | Phase 0 hand-edit (§4) adds `dpp-protocol-http` and reorders `normal_deps` to TOML order. Verified against `deps/dpp.toml` file order (lines 14, 23, 26, 29, 32, 35) + denops.vim last: post-Phase-0 list = `[dpp-ext-toml, dpp-ext-local, dpp-ext-installer, dpp-ext-packspec, dpp-protocol-git, dpp-protocol-http, vim-denops/denops.vim]`, byte-identical to §5.5 sample (design lines 309-317). **Eager-load safety:** `load_plugins` (`dpp_loader.lua:70-78`) only clones via `git clone` (lines 58-61) + `vim.opt.runtimepath:prepend` (line 76); it does NOT `:runtime!` `plugin/*.vim` files, so no sourced side effects fire early. `dpp-protocol-http` is symmetric to `dpp-protocol-git` (already in `normal_deps` today at `dpp_loader.lua:34`) — both are dpp protocol plugins whose denops entrypoint registers a dispatcher; harmless when registered early (no fetch until dpp invokes it via `getProtocols()` in `makeState`). `dpp.make_state` (line 107/119) input unchanged (takes `(dpp_cache_home, dpp_denops_script)`, not the Lua lists). C4 (denops.vim last) folded in: preserved in both Phase 0 description and §5.5. |
| A3/D8 | HIGH/MEDIUM | RESOLVED | §5.7 (design line 352) explicitly drops the in-body date line. `09-dev-workflow.md` line 39 records G3 removal with rationale (violated H5 — system-clock-derived date changes even when inputs are unchanged). Q6 resolution (§7, design line 431) revised. No stale date-line references in design/spec. (No plugin-loading implication; C-side confirmation only.) |
| D1 | HIGH | RESOLVED | Issue body (`docs/issues/2026-07-13-deps-docs-auto-gen.md` lines 28-34) widened to 3 goals: (1) fix drift, (2) generate human-readable docs, (3) establish generator framework. Check-only alternative explicitly rejected (line 34). Design §8 D1 decision matches. (No plugin-loading implication; C-side confirmation only.) |
| D4 | HIGH | RESOLVED | `09-dev-workflow.md` §3 carries only R6, R7, R8, R9, H5 (5 rules). Deferred rules listed explicitly (line 41): R1-R5, R10, G1, G2, G4, H1-H4, carried as invariants I1-I8 in design. No rule duplicated in a way that could drift. G3 removed (A3/D8). `--no-remote` dropped from `gen` task (line 53) per D7. (No plugin-loading implication; C-side confirmation only.) |

## New findings (pass 2 only)

| ID | Severity | Status | Location | Note |
|----|----------|--------|----------|------|
| C-pass2-1 | INFO | open | §3.3 I7 | I7 C5 note wording slightly imprecise. "the generator's cache strategy does not affect denops's deno" — the generator and denops share the same deno cache *directory* (`DENO_DIR` / `~/.cache/deno`), so a careful reader could parse "cache" as "cache directory" and find the statement literally false. The intended frame is "cache *strategy* / first-run-fetch behavior", which is correct (separate processes, separate dependency surfaces; pre-caching `@std/toml` does not pre-cache `@shougo/dpp-vim`/`@denops/std`/etc.). Optional tightening: "pre-caching the generator's `@std/toml` does not pre-cache denops's deps (`@shougo/dpp-vim`, `@denops/std`, etc.) — the two deno processes share `DENO_DIR` but have independent dependency surfaces". Not blocking. |
| C-pass2-2 | INFO | open | §1.2 S7 | S7 heading "No plugin-runtime behavior change" could be misread as applying to the whole design (Phase 0 + Phase 1), but Phase 0 *does* change bootstrap behavior (eagerly loads `dpp-protocol-http` via `load_plugins(normal_deps)` at `dpp_loader.lua:99`). Body text scopes correctly ("byte-identical to the hand-maintained lists **as corrected by Phase 0**") and Phase 0 acceptance acknowledges the change ("`dpp-protocol-http` is cloned and on rtp"). Internally consistent; heading could be clearer (e.g. "No plugin-runtime behavior change in Phase 1 (refactor)"). Not blocking. |

## Verified premises (no action needed)

- **C-pass2-1 (A2/C2 hand-edit correctness):** `deps/dpp.toml` file order is `dpp.vim` (line 5), `dpp-ext-lazy` (line 10), `dpp-ext-toml` (line 14), `dpp-ext-local` (line 23), `dpp-ext-installer` (line 26), `dpp-ext-packspec` (line 29), `dpp-protocol-git` (line 32), `dpp-protocol-http` (line 35). Filtering out `MINIMUM_REPOS = {dpp.vim, dpp-ext-lazy}` and appending `vim-denops/denops.vim` from `deps/denops.toml` (line 3) last yields exactly §5.5's `normal_deps` sample (design lines 309-317). Phase 0 hand-edit is the minimal edit that makes Phase 1 mechanical: (a) add `dpp-protocol-http`, (b) reorder to TOML file order. Current `normal_deps` (`dpp_loader.lua:29-36`) is `[installer, local, packspec, toml, protocol-git, denops.vim]` — missing `dpp-protocol-http` and in a different (non-TOML) order. Phase 0 reconciles both.
- **C-pass2-2 (eager-load safety):** `load_plugins` (`dpp_loader.lua:70-78`) iterates the list and for each entry: (1) clones via `git clone --filter blob:none` if `is_plugin_ready` returns false (lines 73-75 → `install_github_plugin` at lines 56-67), (2) `vim.opt.runtimepath:prepend(vim.fs.normalize(plugin_dest_path))` (line 76). No `:runtime!`, no `:source`, no autocmd setup. `dpp-protocol-http`'s `plugin/*.vim` files (if any) are NOT sourced by `load_plugins`; they'd be sourced by dpp's state file on `dpp#load_state` (`dpp_loader.lua:97`), same as today. The only "early" observable effect is that denops.vim's scan of rtp for `denops/` directories (triggered by `runtime! plugin/denops.vim` at `dpp_loader.lua:82` inside `ensure_denops_plugin()`, called at line 100) would find `dpp-protocol-http`'s denops entrypoint and register its dispatcher — harmless (registration only, no network fetch until dpp invokes the protocol via `getProtocols()` in `makeState`). `dpp-protocol-git` already undergoes this same treatment today (it's in `normal_deps` at `dpp_loader.lua:34`); `dpp-protocol-http` is its symmetric counterpart.
- **C-pass2-3 (C4 fold-in):** `vim-denops/denops.vim` is last in both Phase 0 description (§4: "...protocol-git, protocol-http, denops.vim") and §5.5 sample (design line 316). `load_plugins` rtp-prepends in list order, so denops.vim (last prepended) ends up at the front of rtp. `ensure_denops_plugin()` (`dpp_loader.lua:100`) → `vim.cmd([[runtime! plugin/denops.vim]])` (line 82) finds denops.vim's `plugin/denops.vim` first. ✅ Phase 0 preserves this.
- **C-pass2-4 (C5 scope wording):** I7 (design line 159) correctly notes the C5 scope limit: the generator's deno (one-shot `deno run` for `gen_deps.ts`) vs. denops's separate deno process (`dpp_loader.lua:82` `runtime! plugin/denops.vim` → denops server spawn). The dependency surfaces are correctly listed (`@shougo/dpp-vim`, `@denops/std`, etc. for denops; `@std/toml` for the generator). With `--no-remote` dropped, the note's reference to "cache strategy" is slightly stale (there's no longer a strategy *difference* — both use the default HTTP cache), but the core point (separate processes, separate deps, generator's `@std/toml` cache doesn't help denops) remains accurate. See C-pass2-1 for the wording nit.
- **C-pass2-5 (C8 note accuracy):** §5.9 note (design line 391) is accurate. On classification failure at pre-commit, the generator throws inside `classify()` (§5.4, the `throw new Error(...)` at the duplicate-detection check) BEFORE the render/write step (`render_lua`/`render_readme`/`render_reference` run after classification succeeds). Outputs on disk (`lua/dpp_min_deps.lua`, `deps/README.md`, `docs/references/deps-list.md`) are untouched. The pre-commit hook sees non-zero exit (code 4 or 6), prints error, exits 1 — the commit is blocked. Neovim startup continues to `require("dpp_min_deps")` (§4 Phase 1 rewire) which loads the existing valid module from disk. Neovim starts unchanged from the last successful generation. The note's framing ("generation-time guards preventing classification failures from reaching runtime") is correct.
- **C-pass2-6 (D5 trim reachability):** Walked through the C-7 scenario from pass 1 (`vim-denops/denops.vim` added to `deps/dpp.toml`): (1) `dppEntries` includes the new `vim-denops/denops.vim`; (2) `model.minimum_deps = dppEntries.filter(MINIMUM_REPOS.has).map(.repo)` = `[dpp.vim, dpp-ext-lazy]` (denops.vim not in MINIMUM_REPOS); (3) `model.normal_deps = dppEntries.filter(!MINIMUM_REPOS.has).map(.repo)` = `[dpp-ext-toml, ..., dpp-protocol-http, vim-denops/denops.vim]` (from dpp.toml); (4) `denopsVim = model.by_toml.denops.find(...)` finds denops.vim in `deps/denops.toml`; (5) `model.normal_deps.push(denopsVim.repo)` pushes `vim-denops/denops.vim` again → `model.normal_deps` now has it twice; (6) `all = [...minimum_deps, ...normal_deps]` has `vim-denops/denops.vim` twice; (7) `new Set(all).size !== all.length` → true → throws `repo classified twice in minimum_deps/normal_deps: ...`. The 3-line trimmed assertion (§5.4, design lines 284-287) IS reachable and correct in the C-7 scenario. The complementary predicate (`filter(MINIMUM_REPOS.has)` / `filter(!MINIMUM_REPOS.has)`) guarantees every `dppEntries` item lands in exactly one bucket by construction, so the trimmed check only needs to guard the denops.vim-push and duplicate-`[[plugins]]` cases — which it does.
- **C-pass2-7 (S7 internal consistency):** S7 body ("byte-identical to the hand-maintained lists **as corrected by Phase 0**") ↔ Phase 0 acceptance ("the hand-maintained `normal_deps` now matches what the Phase 1 generator will produce") ↔ Phase 1 acceptance ("prints the same two lists as the post-Phase-0 hand-maintained tables (S7)") — all three reference the same post-Phase-0 baseline. Mutually consistent. The strict sub-claim ("`dpp#make_state` input byte-identical") holds regardless because `dpp.make_state` (`dpp_loader.lua:107, 119`) takes `(dpp_cache_home, dpp_denops_script)`, not the Lua lists; the lists feed only `load_plugins` (lines 92, 99). See C-pass2-2 for the heading-scope nit.
- **No `after/` / `ftplugin/` fire-order interference:** Confirmed in pass 1 (C-6) and unchanged in pass 2: no `after/` or `ftplugin/` directory exists at the repo root. The only ftplugin content is `lua/dpp_hooks/ft.lua`, referenced by `deps/merge.toml`'s non-dpp `hook_files` key and consumed via dpp's `hooksFile` mechanism at `makeState` time (per `docs/references/dpp-hooks-file.md`), not at sentinel-rewrite time. The `dpp-ext-toml` `CursorHold */rc/*.toml` autocmd (`deps/dpp.toml` line 18) is guarded by `if !has('nvim')` (Vim-only) and the glob is `rc/*.toml` not `deps/*.toml`. No startup-autocmd interference with the generator or the Phase 0 hand-edit.

## Open questions back to author

- C-pass2-1 (INFO): tighten I7 C5 note wording to avoid the "cache" (directory vs. strategy) ambiguity? Optional; current wording is correct in intent.
- C-pass2-2 (INFO): consider scoping S7 heading to "No plugin-runtime behavior change in Phase 1 (refactor)" to avoid misreading that it applies to Phase 0? Optional; body text already disambiguates via "as corrected by Phase 0".
