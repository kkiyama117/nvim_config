-- generated by rvpm hook converter; do not edit by hand
-- P1/P4: register denops ddu (app.ts layout)
if vim.fn.exists('*denops#plugin#load') ~= 1 then
  return
end
if vim.fn['denops#plugin#is_loaded']('ddu') == 1 then
  return
end
for _, rtp in ipairs(vim.opt.rtp:get()) do
  if rtp:match('/ddu%.vim$') then
    local main = rtp .. '/denops/ddu/app.ts'
    if vim.fn.filereadable(main) == 1 then
      pcall(vim.fn['denops#plugin#load'], 'ddu', main)
      pcall(vim.fn['denops#plugin#wait'], 'ddu', { silent = 1 })
    end
    break
  end
end
